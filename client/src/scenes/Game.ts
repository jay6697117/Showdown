import Phaser from "phaser";

export class Game extends Phaser.Scene {
  constructor() {
    super("Game");
  }

  create() {
    // Game scene placeholder — will be expanded in Task 3+
  }

  update(_time: number, _delta: number) {
    // Main game loop
  }
}
